# Route Table for Workload-SN
resource "azurerm_route_table" "workload_route_table" {
  name                = "rt-workload-sn"
  location            = azurerm_resource_group.rg.location
  resource_group_name = azurerm_resource_group.rg.name

  route {
    name                   = "default-to-firewall"
    address_prefix         = "0.0.0.0/0"
    next_hop_type          = "VirtualAppliance"
    next_hop_in_ip_address = var.firewall_private_ip
  }
}

# Associate Route Table with Workload-SN
resource "azurerm_subnet_route_table_association" "workload_sn_assoc" {
  subnet_id      = azurerm_subnet.spoke_subnet.id
  route_table_id = azurerm_route_table.workload_route_table.id
}

resource "azurerm_route_table" "dmz_route_table" {
  name                = "rt-dmz-sn"
  location            = azurerm_resource_group.rg.location
  resource_group_name = azurerm_resource_group.rg.name

  route {
    name                   = "default-to-firewall"
    address_prefix         = "0.0.0.0/0"
    next_hop_type          = "VirtualAppliance"
    next_hop_in_ip_address = var.firewall_private_ip
  }
}

resource "azurerm_subnet_route_table_association" "dmz_sn_assoc" {
  subnet_id      = azurerm_subnet.dmz_subnet.id
  route_table_id = azurerm_route_table.dmz_route_table.id
}
